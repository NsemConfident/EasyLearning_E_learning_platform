; Increase PHP upload limits so Past Question PDFs up to 20MB can be uploaded in Filament.
; Only applies when PHP uses .user.ini (e.g. some shared hosting). For php artisan serve or
; custom PHP-FPM, set these in php.ini or your server config.
upload_max_filesize = 25M
post_max_size = 26M
